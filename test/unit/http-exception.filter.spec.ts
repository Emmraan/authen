import { HttpExceptionFilter } from '../../src/common/filters/http-exception.filter'

describe('HttpExceptionFilter (unit smoke)', () => {
    it('is defined', () => {
        expect(HttpExceptionFilter).toBeDefined()
    })
})
