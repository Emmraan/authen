import { AuthController } from '../../src/modules/auth/auth.controller'

describe('AuthController (unit smoke)', () => {
    it('is defined', () => {
        expect(AuthController).toBeDefined()
    })
})
